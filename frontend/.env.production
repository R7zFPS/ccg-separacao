# =====================================================
# VARIÁVEIS DE AMBIENTE — FRONTEND (PRODUÇÃO)
#
# Usado pelo Vite no build de produção (Vercel roda
# `npm run build`, que carrega este arquivo automaticamente).
#
# Backend hospedado no Render (definido em render.yaml,
# serviço "ccg-separacao-backend"):
# =====================================================

VITE_API_URL=https://ccg-separacao-backend.onrender.com
